export enum tagTypes {
  organizer = "organizer",
  event = "event",
}
export const tagTypesList = Object.values(tagTypes);

export enum METHOD {
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  GET = "GET",
  PATCH = "PATCH",
}
