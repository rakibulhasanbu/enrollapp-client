export enum tagTypes {
  organizer = "organizer",
}
export const tagTypesList = Object.values(tagTypes);

export enum METHOD {
  POST = "POST",
  PUT = "PUT",
  DELETE = "DELETE",
  GET = "GET",
  PATCH = "PATCH",
}
